version: "2"

linters:
  settings:
    ireturn:
      allow:
        - error
        - stdlib
        - github.com/gliderlabs/ssh.Signer
  enable:
    - gocritic
    - gocognit
    - gocyclo
    - maintidx
    - dupl
    - mnd
    - unparam
    - ireturn
    - goconst
    - errcheck
  exclusions:
    rules:
      - path: _test\.go
        linters:
          - goconst
          - dupl
          - mnd
          - ireturn
          - unparam
